{
  "name": "photo-session-connect",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "live-server . --port=8081 --open=/",
    "start": "live-server . --port=8081",
    "build": "rm -rf public && mkdir -p public && (shopt -s dotglob && cp -r * public/ 2>/dev/null || find . -maxdepth 1 -not -name '.' -not -name 'public' -not -name 'node_modules' -not -name '.git' -not -name '.vercel' -exec cp -r {} public/ \\;) && echo 'Build process completed'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
