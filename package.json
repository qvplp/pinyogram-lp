{
  "name": "photo-session-connect",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "live-server . --port=8081 --open=/",
    "start": "live-server . --port=8081",
    "build": "rm -rf public && mkdir -p public && cp -r event contest models about shared assets images css js data public/ && cp *.html *.xml *.txt public/ 2>/dev/null || true && test -d public && echo 'SUCCESS: public directory exists' || (echo 'ERROR: public directory not created' && exit 1)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
